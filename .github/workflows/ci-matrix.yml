name: CI Matrix

# Multi-OS test runner. Runs core Rust tests on Linux, macOS, and Windows
# in parallel. Windows-specific steps (signtool, secret-scan PowerShell
# script) remain in ci.yml.

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  rust:
    name: rust (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-15, windows-2025-vs2026]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry & target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: '.'

      - name: cargo fmt
        if: matrix.os == 'ubuntu-24.04'  # run once; format is OS-independent
        run: cargo fmt --all -- --check

      - name: cargo clippy
        # -D warnings treats every warning as an
        # error, matching the .cargo/security.yaml
        # `deny = "warnings"` setting. We pin the
        # toolchain to `stable` (dtolnay/rust-toolchain
        # above) to make builds reproducible.
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: cargo test (Unix)
        if: matrix.os != 'windows-2025-vs2026'
        run: cargo test --workspace

      - name: cargo test (Windows)
        if: matrix.os == 'windows-2025-vs2026'
        # Windows: skip tests that require DPAPI / Windows-specific features
        run: cargo test --workspace

      - name: cargo build (release)
        run: cargo build --release --workspace

      - name: cargo audit
        if: matrix.os == 'ubuntu-24.04'  # run once
        # --locked pins to the version in
        # Cargo.lock so CI is reproducible.
        run: cargo install cargo-audit --locked && cargo audit

      - name: cargo deny
        if: matrix.os == 'ubuntu-24.04'  # run once; pinned in ci.yml too
        # 0.19.8 is the last 0.19.x release that
        # supports `vulnerability = "deny"`. Newer
        # 0.20.x removed that key.
        run: cargo install cargo-deny --locked --version 0.19.8 && cargo deny check
